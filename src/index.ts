import foreach from "./foreach";
import map from './map'
import filter from "./filter";
import toArray from "./toArray";
export default {
  foreach,
  map,
  filter,
  toArray
};

export {
  foreach,
  map,
  filter,
  toArray
};